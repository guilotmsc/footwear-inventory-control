<%= form_with(model: numbering, local: true) do |form| %>
  <% if numbering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(numbering.errors.count, "error") %> prohibited this numbering from being saved:</h2>

      <ul>
      <% numbering.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :numbering_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
